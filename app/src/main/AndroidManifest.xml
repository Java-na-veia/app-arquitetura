<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoGerenciamentoDeArquitertura"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Declaração da ConsultaProjetoActivity -->
        <activity
            android:name=".ConsultaProjetoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaração da MainActivity -->
        <activity
            android:name=".TelaProjetoActivity"
            android:exported="true">
            <!-- Se esta for a tela inicial, adicione o filtro de intenção -->
        </activity>

    </application>

</manifest>
